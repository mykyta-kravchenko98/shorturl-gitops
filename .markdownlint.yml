default: true
MD013: false   # line-length - docs here have long command examples/URLs
MD033: false   # allow raw HTML if ever needed
MD041: false   # first line doesn't have to be a top-level heading (front matter etc.)
