replicaCount: 1
image:
  repository: 528081867341.dkr.ecr.eu-central-1.amazonaws.com/shorturl
  tag: 1426ccd6b9731b203881caabdf25ca1a9d7c4b1e
  digest: "sha256:c014c8301a16c8a25cc1673d8478fe4be4d71a1dba4c476927b27ab4440c169f"
  pullPolicy: IfNotPresent
service:
  port: 8585
resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 500m
    memory: 128Mi
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 75
otel:
  gatewayEndpoint: otel-collector-gateway.observability.svc.cluster.local:4317
  sidecar:
    enabled: true
    image: otel/opentelemetry-collector-contrib:0.105.0
    resources:
      requests:
        cpu: 20m
        memory: 64Mi
      limits:
        cpu: 250m
        memory: 128Mi
postgres:
  # When true, this chart also deploys Postgres itself (plain StatefulSet,
  # no operator - see templates/postgres-*.yaml). Set false and point
  # `host` at an external instance (e.g. a managed DB) instead.
  deployed: true
  image: postgres:16-alpine
  storage: 1Gi
  host: postgres
  port: "5432"
  dbName: ShortURLDataDB
  sslMode: disable
  credentials:
    secretName: postgres-credentials
  migrations:
    enabled: true
  migrateImage:
    repository: 528081867341.dkr.ecr.eu-central-1.amazonaws.com/shorturl-migrate
    tag: 1426ccd6b9731b203881caabdf25ca1a9d7c4b1e
    digest: "sha256:88fbd128a4dbe3f8d97686e2aefb9221be95618bde1c048eb6eaa5a7dc5dde1a"
secretsManagement:
  awsRegion: eu-central-1
  postgresSecretName: shorturl/postgres
  credentialsSecretName: aws-secretstore-credentials
  secretStore:
    create: true
    name: aws-secretsmanager
  externalSecret:
    enabled: true
    secretStoreName: aws-secretsmanager
    secretStoreKind: SecretStore
    targetSecretName: postgres-credentials-pending
    refreshInterval: 1h
  credentialRotation:
    enabled: true
    pendingSecretName: postgres-credentials-pending
imagePullSecret:
  enabled: true
  name: ecr-pull-secret
ecrRefresh:
  enabled: true
  region: eu-central-1
  registry: 528081867341.dkr.ecr.eu-central-1.amazonaws.com
  secretName: ecr-pull-secret
  credentialsSecretName: aws-ecr-credentials
  schedule: "0 */10 * * *"
server:
  restPort: "8585"
  dataCenterId: 1
  machineId: 1
ingress:
  enabled: false
  host: shorturl.local
metrics:
  serviceMonitor:
    enabled: false
probes:
  liveness:
    path: /healthz
  readiness:
    path: /readyz
