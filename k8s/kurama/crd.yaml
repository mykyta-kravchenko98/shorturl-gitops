# Auto-synced by Kurama's CI from config/crd/bases on every push to main.
# Do not edit this copy directly.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: trafficscenarios.traffic.kurama.dev
spec:
  group: traffic.kurama.dev
  names:
    kind: TrafficScenario
    listKind: TrafficScenarioList
    plural: trafficscenarios
    singular: trafficscenario
    shortNames:
      - ts
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Phase
          type: string
          jsonPath: .status.phase
        - name: Target
          type: string
          jsonPath: .spec.target.baseURL
      schema:
        openAPIV3Schema:
          type: object
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              type: object
              required:
                - target
              properties:
                suspend:
                  type: boolean
                target:
                  type: object
                  required:
                    - baseURL
                  properties:
                    baseURL:
                      type: string
                      pattern: '^https?://.+'
            status:
              type: object
              properties:
                phase:
                  type: string
                message:
                  type: string
                observedGeneration:
                  type: integer
                  format: int64
